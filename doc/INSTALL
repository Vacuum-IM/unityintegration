Сборка выполняется стандартно:

qmake:

qmake INSTALL_PREFIX=/usr unityintegration.pro
make
sudo make install

cmake:

mkdir build && cd build
cmake -DCMAKE_INSTALL_PREFIX=/usr ..
make
sudo make install

